interface:
  display_name: "SPDD Sync"
  short_description: "Sync implementation back to the prompt"
  brand_color: "#0F766E"
  default_prompt: "Use $spdd-sync on spdd/prompt/EXAMPLE.md after this refactor."

policy:
  allow_implicit_invocation: false
