interface:
  display_name: "SPDD API Test"
  short_description: "Generate API verification artifacts"
  brand_color: "#0F766E"
  default_prompt: "Use $spdd-api-test for the billing usage endpoints."

policy:
  allow_implicit_invocation: false
